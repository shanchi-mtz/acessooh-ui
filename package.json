{
  "name": "acessooh-ui",
  "private": true,
  "version": "0.0.5",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "publish": "git subtree push --prefix dist origin gh-pages",
    "publish:force": "git push origin $(git subtree split --prefix dist main):gh-pages --force",
    "deploy2": "npm run build && git push origin $(git subtree split --prefix dist main):gh-pages --force",
    "deploy": "echo 🚀 Iniciando deploy... && npm run build && git subtree push --prefix dist origin gh-pages && echo ✅ Deploy concluído com sucesso!",
    "ciro": "npm run build && git subtree split --prefix dist main -b publish-tmp && git push -f origin publish-tmp:gh-pages && git branch -D publish-tmp"
  },
  "dependencies": {
    "@turf/turf": "^7.2.0",
    "framer-motion": "^11.2.6",
    "leaflet": "^1.9.4",
    "lucide-react": "^0.460.0",
    "mapbox-gl": "^3.15.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-leaflet": "^4.2.1",
    "react-map-gl": "^7.1.9",
    "react-simple-maps": "^3.0.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0",
    "@vitejs/plugin-react": "^4.2.1",
    "autoprefixer": "^10.4.17",
    "gh-pages": "^6.3.0",
    "postcss": "^8.4.35",
    "tailwindcss": "^3.4.7",
    "vite": "^5.2.0",
    "vite-plugin-commonjs": "^0.10.4"
  }
}
